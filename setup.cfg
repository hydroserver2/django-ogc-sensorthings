[metadata]
name = django-sensorthings
version = 0.1.0

[options]
python_requires = >=3.10
package_dir=
    =src
packages = find:
zip_safe = True
install_requires =
    django >= 4.2
    django-ninja >= 1.0
    pydantic >= 2.7

[options.packages.find]
where=src
